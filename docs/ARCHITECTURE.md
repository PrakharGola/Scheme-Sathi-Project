# Architecture

SchemeSathi uses a Vite React frontend, an Express TypeScript backend, PostgreSQL via Prisma, and Docker for local orchestration. The frontend calls `/api/*` endpoints for auth, scheme listings, eligibility checks, chat, and admin analytics.
